var app = angular.module('InstrumApp');

app.controller('instrumentalController', ['$scope', function($scope) {
    // create a message to display in our view
      $scope.message = 'This is the instruments routes!!';
     }
  ]);
