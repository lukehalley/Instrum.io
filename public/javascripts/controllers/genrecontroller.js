var app = angular.module('InstrumApp');

app.controller('genreController', ['$scope', function($scope) {
    // create a message to display in our view
      $scope.message = 'This is the genre page!!';
     }
  ]);
