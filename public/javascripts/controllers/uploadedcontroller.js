var app = angular.module('InstrumApp');

app.controller('uploadedController', ['$scope', function($scope) {
    // create a message to display in our view
      $scope.message = 'This is the uploads page!!';
     }
  ]);
